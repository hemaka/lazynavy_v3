import { ModuleScreen } from '../../shared/ui/ModuleScreen'

export function MapScreen() {
  return (
    <ModuleScreen
      kicker="MAP"
      title="Landmarks & Discovery"
      body="V3 reuses the V2 POI foundation and adds discovery achievements as a map layer tied to voyage evidence and photo logs."
    />
  )
}
